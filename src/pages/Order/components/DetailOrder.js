import React from "react";

const DetailOrder = (props) => {
  const { data } = props;

  const columns = [
    {
      title: "",
      dataIndex: "image",
      key: "image",
    },
  ];

  return <div>DetailOrder</div>;
};

export default DetailOrder;
